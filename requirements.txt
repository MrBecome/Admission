Django==6.0.8
PyMySQL==1.2.0
python-dotenv==1.2.2
qrcode==8.2
pillow==12.3.0
pandas==3.0.5
openpyxl==3.1.5
whitenoise==6.8.2
# redis is only imported by Django's cache backend if REDIS_URL is set in
# .env (see settings.py CACHES). Safe to leave installed either way.
redis==5.2.1